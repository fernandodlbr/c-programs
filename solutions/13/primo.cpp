#include <stdio_ext.h>
#include <stdlib.h>

#define MAX 70

bool dividir(int dividendo, int posible_primo){
  return !(dividendo % posible_primo);
}

int main(int argc, char *argv[]){

  int primo[MAX];
  int primos_encontrados = 1;
  primo[0] = 2 ;

/*
mostrar 70 primos
  dividir el posible primo entre los otros primos
  añadir el nuevo primo a la lista de primos
*/

for(int candidato = 2; posible_primo<MAX; candidato++;)
  bool posible_primo = true;
    
  return EXIT_SUCCESS;
}
