#include <stdio_ext.h>
#include <stdlib.h>

int main(int argc, char *argv[]){

/*usar sizeof*/
struct list{
  const char *list[]={
    "bread",
    "toast",
    "bacon"
  };
};
const char=sizeof(bread);

  printf("%s", list sizeof(struct list));

  return EXIT_SUCCESS;
}
